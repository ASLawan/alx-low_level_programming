#include <stdio.h>
/**
 * main - Prints the alphabet.
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int number, i;

	number  = 10;
	i = 0;

	while (i < number)
	{
		printf("%d", i);
		i++;
	}
	printf("\n");

	return (0);


}
